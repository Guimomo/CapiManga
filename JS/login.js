document.addEventListener("DOMContentLoaded", () => {

    // const loginButton = document.querySelector(".login-button");
    // const menuLogin = document.querySelector(".menu-login");

    // loginButton.addEventListener("click", () => {
    //     menuLogin.classList.toggle("menu-visible"); // Alterna la clase "menu-visible"
    // });
    
    // Obtener el checkbox
    const loginCheckbox = document.querySelector(".login-checkbox");
    
    // Asegurar que esté desmarcado al cargar la página
    loginCheckbox.checked = false;
});